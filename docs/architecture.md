# Architecture (Draft)
- Spring Boot 3.5 (REST)
- MySQL (JPA), Redis (rate limit/cache-ready)
- Auth: JWT + Firebase + Google
- Containers: docker-compose (app, db, redis)
- OpenAPI: springdoc `/swagger-ui`
